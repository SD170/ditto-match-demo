# Ditto AI — frontend

React (TypeScript) + Vite + Tailwind. **Dev server** proxies `/api` and `/health` to the FastAPI backend on port **8000** — start the API first (see repo root `README.md`).

```bash
npm install && npm run dev
```

Build: `npm run build`. Lint: `npm run lint`.
